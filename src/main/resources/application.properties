# Tells Spring Boot where the database is
spring.datasource.url=jdbc:mysql://localhost:3306/job_tracker

# YOUR MySQL username (usually root)
spring.datasource.username=root

# YOUR MySQL password (change this to your actual password!)
spring.datasource.password=Reshe@2007

# Tells Spring Boot to automatically create the SQL tables for us
spring.jpa.hibernate.ddl-auto=update

# Prints the SQL queries in the terminal so we can see what it's doing (Great for learning!)
spring.jpa.show-sql=true

# JWT Secret Key for the login system security
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970